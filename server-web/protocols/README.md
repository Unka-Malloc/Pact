# server-web/protocols

本目录存放 `server-web` 控制台所属的协议文档。

范围：

- Vue 控制台消费 Node.js 服务端 `/api/*` 时依赖的下游协议约束。
- UI 状态、字段依赖、兼容降级和展示侧错误语义。

当前目录：

- `server/`：控制台消费服务端接口时的下游协议约束。

实现逻辑不放在这里。界面状态、组件和请求调用代码仍放在 `server-web` 的源码目录中。

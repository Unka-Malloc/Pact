# Knowledge Agent Skill

Reusable agent-facing skill contract for evidence-grounded knowledge-base operations. Tools and protocol dependencies are declared in `dependencies.json`; no large evidence payloads are bundled here.
